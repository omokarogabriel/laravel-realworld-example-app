# server {
#     listen 80;
#     server_name _;

#     root /var/www/html/public;
#     index index.php index.html;

#     # Laravel's public directory
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     # Pass PHP requests to PHP-FPM container
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass laravel-container:9000; # matches ECS container name
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }

#     # Deny access to hidden files (.env, .git, etc.)
#     location ~ /\. {
#         deny all;
#     }

#     # Increase client upload size (useful for file uploads)
#     client_max_body_size 100M;
# }





# server {
#     listen 80;
#     server_name _;

#     root /var/www/html/public;
#     index index.php index.html;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass laravel:8000; # ECS name replaced with local Docker service name
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }

#     location ~ /\. {
#         deny all;
#     }

#     client_max_body_size 100M;
# }










# server {
#     listen 80;
#     server_name _; # works with IP

#     # Serve the frontend
#     root /var/www/frontend/dist; # path to built frontend files
#     index index.html;

#     # Frontend routes (SPA fallback)
#     location / {
#         try_files $uri /index.html;
#     }

#     # API proxy to Laravel backend
#     location /api {
#         proxy_pass http://127.0.0.1:9000; # Laravel backend (PHP-FPM or container)
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # If Laravel runs via PHP-FPM in same Nginx
#     location ~ \.php$ {
#         root /var/www/laravel/public; # Laravel public folder
#         include fastcgi_params;
#         fastcgi_pass unix:/var/run/php/php8.2-fpm.sock; # adjust PHP version/socket
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         fastcgi_param DOCUMENT_ROOT $realpath_root;
#     }
# }







# server {
#     listen 80;
#     index index.php index.html;
#     server_name localhost;

#     root /var/www/html/public;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     # location ~ \.php$ {
#     #     include fastcgi_params;
#     #     fastcgi_pass app:9000;   # This matches the "app" service in docker-compose
#     #     fastcgi_index index.php;
#     #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     # }

#     location ~ \.php$ {
#         # include fastcgi.conf;
#         include fastcgi_params;
#         fastcgi_pass app:9000;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#     }


#     location ~ /\.ht {
#         deny all;
#     }
# }



# server {
#     listen 80;
#     index index.php index.html;

#     server_name laravel-app.local;  # Change to your domain or IP

#     root /var/www/html/public;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass laravel:9000;  # "laravel" is the PHP-FPM container name in ECS task definition
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }

#     location ~ /\.ht {
#         deny all;
#     }
# }


server {
    listen 80;
    server_name _;

    root /var/www/html/public;
    index index.php index.html;

    location / {
        proxy_pass http://laravel_app:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ~ \.php$ {
        proxy_pass http://laravel_app:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
